import { DataMeta } from "@carlonicora/nestjs-neo4jsonapi";

export const usageRecordMeta: DataMeta = {
  type: "usage-records",
  endpoint: "usage-records",
  nodeName: "usageRecord",
  labelName: "UsageRecord",
};
