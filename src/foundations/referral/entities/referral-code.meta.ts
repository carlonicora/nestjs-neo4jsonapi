import type { DataMeta } from "../../../common";

export const referralCodeMeta: DataMeta = {
  type: "referral-codes",
  endpoint: "referral-codes",
  nodeName: "referralCode",
  labelName: "ReferralCode",
};
